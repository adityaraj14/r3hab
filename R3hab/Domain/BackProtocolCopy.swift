import Foundation

/// Condensed QL / one-sided low-back protocol (E3-style 80/20). Not medical advice.
enum BackProtocolCopy {
    static let title = "Low back · QL / trunk"

    static let summary = """
    One-sided low back pain is often blamed on the QL, but the exact tissue is hard to isolate. Rehab still works: temporarily ease aggravators, walk, then rebuild side-bend and extension capacity with holds first, then slow dynamic work.
    """

    static let program = """
    2–3×/week (both sides when applicable):
    • Side plank holds 2–3×30–60s (or suitcase hold/carry)
    • Hip thrust or bridge 2–3×8–15 (or extension holds if moving is too hot)
    Most days: walk as tolerated.

    Irritable flare: easy walk, gentle cat-cow/rotations, skip hard side-bend load for a few days.

    Progress: longer holds → more ROM → more load — one variable at a time. Think months, not days.
    """

    static let redFlags = """
    Seek care urgently for progressive leg weakness/numbness, bowel/bladder change, fever, unexplained weight loss, or night pain that doesn’t settle.
    """
}
